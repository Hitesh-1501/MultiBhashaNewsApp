<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MultiBhashaNews" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/light_red</item>
        <item name="colorPrimaryVariant">@color/light_red</item>
        <item name="android:statusBarColor">@color/light_red</item>
    </style>

    <style name="Theme.MultiBhashaNews" parent="Base.Theme.MultiBhashaNews">

        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>

    </style>


    <style name="LoginTextInputOuterFieldStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">15dp</item>
        <item name="android:layout_marginBottom">15dp</item>
        <item name="boxStrokeColor">@color/light_red</item>
        <item name="hintTextColor">@color/light_red</item>
        <item name="android:textColorHint">@color/black</item>
        <item name="passwordToggleTint">@color/light_red</item>
    </style>

    <style name="LoginTextInputInnerFieldStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/light_red</item>
        <item name="android:maxLines">1</item>
        <item name="android:textSize">14sp</item>
        <item name="android:singleLine">true</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Base.Theme.MultiBhashaNews" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="TextColorchange">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="cardBackground">
        <item name="cardBackgroundColor">@color/white</item>
    </style>

    <style name="toolbarAction">
        <item name="android:background">@color/light_red</item>
        <item name="titleTextColor">@color/white</item>
    </style>

    <style name="ViewBag">
        <item name="android:background">@color/white</item>
    </style>

</resources>